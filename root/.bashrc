# User specific aliases and functions

alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

alias wl="/root/Oracle/Middleware/user_projects/domains/DefaultServer/startWebLogic.sh"
alias wlj="/root/Oracle/Middleware/user_projects/domains/DefaultServer/bin/startWeblogic-jrebel.sh"

